app.name=Example REST API
app.version=v1.0.0

# OAuth2
soc.keycloak.certsUrl=http://localhost:3001/realms/mdr-dev/protocol/openid-connect/certs
soc.keycloak.cert.alg=RS256

# Springdoc
springdoc.swagger-ui.enabled=false

# Cache
guava.cache.duration=3

# Liquibase
spring.liquibase.enabled=false

use.dummy.pubkey=true